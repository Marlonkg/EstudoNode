//funciona da mesma forma que o destructuring do objeto que cria variaveis para cada elemento do array seguindo a ordem estabelecida
let nomes = ['jonas', 'maria', 'rodrigo', 'josé']

// esse importa a posição!
let [nome1, nome2, nome3, nome4] = nomes

console.log(nome3)



let numeros = [1, 5, 3, 6, 23, 54, 0, 3]

let [n1, n2, n3, n4, n5, n6, n7, n8] = numeros

console.log(n6)
